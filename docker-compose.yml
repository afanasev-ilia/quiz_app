version: '3.9'
services:
  db:
    container_name: db
    image: postgres:14.0-alpine
    volumes:
      - db_data:/var/lib/postgresql/data/
    restart: always
    env_file:
      - ./.env
    ports:
      - '5432:5432'

volumes:
  db_data: